# MarkItUI - Python-Voraussetzungen
# Installation: pip install -r requirements.txt
# (Die App kann diese Pakete auch selbst per 1-Klick installieren.)

# --- Spezialisierte Konverter-Engines (decken alle Hauptformate eigenstaendig ab) ---
pdfplumber>=0.11.0          # PDF inkl. Tabellen
pypdfium2>=4.30.0           # PDF (schnell, ohne weitere Abhaengigkeiten)
pdfminer.six>=20231228      # PDF (letzte Reserve)
mammoth>=1.8.0              # Word (.docx) -> Markdown
python-docx>=1.1.0          # Word (.docx) inkl. Tabellen
python-pptx>=1.0.0          # PowerPoint (.pptx) inkl. Notizen
openpyxl>=3.1.0             # Excel (.xlsx/.xlsm)
xlrd>=2.0.1                 # Excel (altes .xls-Format)

# --- Text-, HTML- und Markdown-Werkzeuge ---
beautifulsoup4>=4.12.0
markdown>=3.5.0
pygments>=2.17.0
puremagic>=1.20

# --- MarkItDown (Microsoft) als universelle Zusatz-Engine ---
# Die Extras sind zwingend: ohne sie kann MarkItDown 0.1.x weder PDF noch Office-Dateien lesen.
markitdown[docx,pdf,pptx,xlsx,xls]>=0.1.0 ; python_version >= "3.10"
